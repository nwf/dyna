
b := 0.

a += 1/b.

c += "" + b.

e := 0.

% initializer exceptions.
%p += 1+null.
%q max= 3/0.

    b := e/0. a += e/0.

d += null.
d += 1.


a(X) := f(X,Y).

f(1,1) := 1.
f(1,2) := 2.

f(2,1) := 1.
f(2,2) := 2.
